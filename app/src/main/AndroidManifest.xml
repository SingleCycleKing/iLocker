<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avazu.applocker">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="ANDROID.PERMISSION.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.AppList"
            android:label="@string/title_activity_app_list">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.AppStartService"
            android:enabled="true"
            android:exported="false"
            android:persistent="true"
            android:process="com.avazu.applocker.service.AppStartService" />

        <receiver
            android:name=".service.StickyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".view.activity.Lock"
            android:label="@string/title_activity_lock"
            android:launchMode="singleTask" />
        <activity
            android:name=".view.activity.Setting"
            android:label="@string/title_activity_setting"></activity>
        <activity
            android:name=".view.activity.SetPassword"
            android:label="@string/title_activity_set_password"></activity>
    </application>

</manifest>
